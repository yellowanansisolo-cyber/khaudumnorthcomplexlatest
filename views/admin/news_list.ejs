<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .table-container { overflow-x: auto; }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 12px; border: 1px solid var(--color-border); text-align: left; }
        th { background-color: #f8f9fa; }
        .action-btns { display: flex; gap: 10px; }
        .table-img-preview { max-width: 100px; height: auto; border-radius: 4px; }
    </style>
</head>
<body class="admin-body">
    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1 class="dashboard-title"><i class="fas fa-newspaper"></i> <%= title %></h1>
            <div>
                <a href="/admin/dashboard" class="btn-admin me-2">Back to Dashboard</a>
                <a href="/admin/news/add" class="btn-admin btn-save"><i class="fas fa-plus me-2"></i> Add New Article</a>
            </div>
        </div>

        <div class="dashboard-card">
            <div class="table-container">
                <table>
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>Title</th>
                            <th>Date</th>
                            <th>Tag</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (articles.length === 0) { %>
                            <tr><td colspan="5">No articles found.</td></tr>
                        <% } %>
                        <% articles.forEach(article => { %>
                            <tr>
                                <td><img src="<%= article.image %>" alt="<%= article.title %>" class="table-img-preview"></td>
                                <td><%= article.title %></td>
                                <td><%= new Date(article.date).toLocaleDateString() %></td>
                                <td><%= article.tag %></td>
                                <td>
                                    <div class="action-btns">
                                        <a href="/admin/news/edit/<%= article.id %>" class="btn-admin">Edit</a>
                                        <form action="/admin/news/delete/<%= article.id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this article?');">
                                            <button type="submit" class="btn-admin btn-delete" style="background-color: var(--color-danger);">Delete</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>