<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .image-preview { max-width: 200px; max-height: 150px; border: 1px solid #ddd; padding: 5px; margin-bottom: 10px; display: block; background-color: #f0f0f0; }
    </style>
</head>
<body class="admin-body">
    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1 class="dashboard-title"><i class="fas fa-edit"></i> <%= title %></h1>
            <div><a href="/admin/news" class="btn-admin me-2">Back to List</a></div>
        </div>

        <div class="dashboard-card">
            <form action="<%= typeof article !== 'undefined' ? '/admin/news/edit/' + article.id : '/admin/news/add' %>" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" class="form-control" required value="<%= typeof article !== 'undefined' ? article.title : '' %>">
                </div>
                <div class="form-group">
                    <label for="date">Date</label>
                    <input type="date" id="date" name="date" class="form-control" required value="<%= typeof article !== 'undefined' ? article.date : new Date().toISOString().split('T')[0] %>">
                </div>
                <div class="form-group">
                    <label for="tag">Tag / Category (e.g., Community, Conservation)</label>
                    <input type="text" id="tag" name="tag" class="form-control" required value="<%= typeof article !== 'undefined' ? article.tag : '' %>">
                </div>
                <div class="form-group">
                    <label for="image">Image</label>
                    <% if (typeof article !== 'undefined' && article.image) { %>
                        <img src="<%= article.image %>" alt="Current Image" class="image-preview">
                        <input type="hidden" name="currentImage" value="<%= article.image %>">
                    <% } %>
                    <input type="file" id="image" name="image" class="form-control" accept="image/*" <% if (typeof article === 'undefined') { %>required<% } %>>
                    <small class="form-text text-muted">Upload a new image to replace the current one. Required for new articles.</small>
                </div>
                <div class="form-group">
                    <label for="content">Content</label>
                    <textarea id="content" name="content" class="form-control" rows="10" required><%= typeof article !== 'undefined' ? article.content : '' %></textarea>
                </div>

                <button type="submit" class="btn-admin btn-save">
                    <i class="fas fa-save me-2"></i> <%= typeof article !== 'undefined' ? 'Update' : 'Create' %> Article
                </button>
            </form>
        </div>
    </div>
</body>
</html>